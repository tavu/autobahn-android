<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent" android:background="@android:color/white">

	<TextView
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:textSize="17dp"
        android:id="@+id/header" android:textColor="@color/textColor"
        android:textIsSelectable="false" android:gravity="center" android:layout_margin="30dp"
        android:layout_gravity="center"/>
	<!--    android:id="@+id/textView" android:layout_gravity="center" android:singleLine="false" -->

	<ListView
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:id="@+id/listView"
		android:layout_gravity="center" android:divider="@drawable/gradient_divider" android:dividerHeight="1dp"
        android:drawSelectorOnTop="false"/>
</LinearLayout>